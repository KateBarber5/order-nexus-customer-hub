{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://order.govmetric.ai/*"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*", "/js/*", "/api/*"]
  }
}